# mermaid-svg-css-vars

See **README.adoc** for the full documentation.

Intermediate library: rewrite Mermaid SVG theme colors to `var(--mermaid-*, fallback)` and normalize SVGs for responsive web embedding.

```bash
pnpm add @openshellorg/mermaid-svg-css-vars
```
